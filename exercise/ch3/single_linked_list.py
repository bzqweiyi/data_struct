class ClassName(object):
	"""docstring for ClassName"""
	def __init__(self, arg):
		super(ClassName, self).__init__()
		self.arg = arg
		
	def ClearList(LinkList):
		p = iter(LinkList)
		while p not :
			q = p.next
			p = null
			p = q

